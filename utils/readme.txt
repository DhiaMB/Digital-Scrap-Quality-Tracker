After uploading the file, the user tells the app what each column means.
Sometimes the column names could be different (example: Scrap_M vs Quantity_Scrapped_meters).
By assigning roles, the app knows exactly what to use for KPIs and charts.